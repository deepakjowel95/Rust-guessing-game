# This is a number guessing game built on rust

## Follow the step to run on native system
- install rust
     `curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh`
- confirm and check version
  `rustc --version`
